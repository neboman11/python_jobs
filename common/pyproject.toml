[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "jobs-common"
version = "1.1.1"
description = "Common utilities for Python jobs"
requires-python = ">=3.12"
dependencies = ["requests==2.32.4"]
